apiVersion: v1
kind: PersistentVolume
metadata:
  name: foxlink-nfs-pv
spec:
  capacity:
    storage: 500Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: 192.168.x.x          # TODO: 替換為 Synology NAS IP
    path: /volume1/foxlink-uploads  # TODO: 替換為實際共用資料夾路徑
  mountOptions:
    - nfsvers=4.1
    - hard
    - timeo=600
    - retrans=3
    - noatime
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: foxlink-nfs-pvc
  namespace: foxlink
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  volumeName: foxlink-nfs-pv
