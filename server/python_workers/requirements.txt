# PDF → DOCX worker dependencies
# Pin minor versions:pdf2docx 對 PyMuPDF 升級敏感(1.25+ 改 import path 會炸)
PyMuPDF>=1.23.0,<1.25.0
pdf2docx==0.5.8

# Phase 2:Vision rebuild — python-docx 組原生 DOCX(table + cell shading + merge)
# Pillow 給 add_picture 用(實際是 python-docx 在 add_picture 時 lazy import)
python-docx>=1.1.0,<2.0.0
Pillow>=10.0.0,<12.0.0
